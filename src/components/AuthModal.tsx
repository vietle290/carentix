import React from 'react'

type AuthModalProps = {
    open: boolean;
    onClose: () => void;
}

function AuthModal({open, onClose}: AuthModalProps) {
  return (
    <div>
      
    </div>
  )
}

export default AuthModal
